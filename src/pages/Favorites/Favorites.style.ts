import { colors } from '@styles/theme'
import styled from 'styled-components'

export const Title = styled.h2`
  width: 100%;
  padding: 2rem 0 1rem;
  font-size: 2rem;
  text-align: center;
  color: ${colors.white};
`
